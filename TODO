FIX WAITRESS??
DEPLOYMENT AND LOCKING DOWN
COMMENT CODE
Deployment:
    Install Mongo
    Create odychk.results db/col
    Install nginx + waitress
    Tell flask it's behind a proxy
    Once launched, wait for log to show that db has been populated

REQUIRES:
    Python 3.10+